# multer-storage-cloudinary@4 declares a peer dep on cloudinary@^1, but it works
# correctly with cloudinary@2 (which we need for the security patch). Allow the
# known-benign peer mismatch so `npm ci` succeeds on CI / Render.
legacy-peer-deps=true
